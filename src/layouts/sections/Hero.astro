<div class="bg-neutral-900 w-full min-h-screen flex justify-center items-center">
  <div class="container px-6">
      <div class="flex flex-col gap-4 items-center justify-center relative">
          <h1 class="text-6xl text-white font-black text-center flex flex-col md:flex-row justify-center items-center md:items-baseline md:gap-4">
              &ltACM WEB /&gt <span class="font-light text-3xl">at Missouri S&T</span>
          </h1>
          <p class="text-white text-md font-light font-roboto text-center">
              Welcome to the ACM Web page. We are a group of students who are passionate about web development and design. We are here to help you learn and grow in the field of web development. We offer workshops, tutorials, and resources to help you get started with web development.
              <br>
              <span class="font-medium">
                  Whether you are a beginner or an expert, we have something for 
                  <span class="underline" id="random-text">everyone</span>
              </span>
          </p>
          <hr>
          <button id="start-button" class="px-8 py-2 text-white text-xl font-medium rounded-full bg-neutral-800 click:scale-90 hover:text-neutral-800 hover:bg-white hover:scale-105 transition-all duration-150">
              Start
          </button>
          <div class="float absolute -top-1/2 text-white font-robotocondensed text-lg font-light hidden md:block">&ltdiv&gt</div>
          <div class="float absolute -top-1/3 right-1/4 text-white font-robotocondensed text-lg font-light hidden md:block">&ltnav&gt</div>
          <div class="float absolute -top-1/3 left-1/4 text-white font-robotocondensed text-lg font-light hidden md:block">&ltsection&gt</div>
          <div class="float absolute -bottom-1/2 text-white font-robotocondensed text-lg font-light hidden md:block">&ltp1&gt</div>
          <div class="float absolute -bottom-1/3 right-1/4 text-white font-robotocondensed text-lg font-light hidden md:block">&ltp2&gt</div>
          <div class="float absolute -bottom-1/3 left-1/4 text-white font-robotocondensed text-lg font-light hidden md:block">&ltbody&gt</div>
      </div>
  </div>
</div>

<script>
  const variants = ['everyone', 'designers', 'enthusiasts', 'programmers', 'developers', 'students', 'creators', 'innovators', 'entrepreneurs', 'educators'];
  const textElement = document.getElementById('random-text') as HTMLSpanElement;
  const startButton = document.getElementById('start-button') as HTMLButtonElement;
  const floatingElements = document.querySelectorAll('.float');
  const max = 5
  const min = 3

  startButton.addEventListener('click', function() {
      // Cool animation

      window.location.href = '/about';
  });

  floatingElements.forEach(element => {
      const randomDuration = (Math.random() * (max - min + 1)) + min;
      (element as HTMLElement).style.animationDuration = `${randomDuration}s`;
  });

  let currentIndex = 0;
  setInterval(() => {
      textElement.textContent = variants[currentIndex];
      currentIndex = (currentIndex + 1) % variants.length;
  }, 2000);
</script>

<style>
  .float {
      animation: float 4s ease-in-out infinite;
  }

  @keyframes float {
      0%, 100% {
          transform: translateY(0);
      }
      50% {
          transform: translateY(-10px);
      }
  }
</style>